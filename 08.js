function calcPetFood(dogFood, catFood) {
  const dogFoodPrice = 2.5;
    const catFoodPrice = 4;
    const total = (dogFood * dogFoodPrice) + (catFood * catFoodPrice);
console.log(total + ' lv.'); // Output: undefined
}
calcPetFood(5, 4); // Output: undefined